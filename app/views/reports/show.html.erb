<p>
  <strong>Title:</strong>
  <%= @report.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @report.content %>
</p>

<p>
  <strong>User:</strong>
  <%= @report.user_id %>
</p>

<% if @report.user == current_user %>
  <%= link_to 'Edit', edit_report_path(@report) %> |
<% end %>

<%= render partial: 'comments/comments', locals: {comment: @comment, comments: @comments} %>

<%= link_to 'Back', reports_path %>
